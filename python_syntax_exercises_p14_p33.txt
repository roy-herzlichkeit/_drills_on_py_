p14. Nested unpacking
Write 5 mini tasks that use tuple unpacking, starred expressions, and nested unpacking.
Each task should force you to read structure carefully before writing code.

p15. Truthy and falsy logic
Write 5 tasks that combine booleans, empty values, short-circuiting, and conditional expressions.
Make the reader reason about what each branch returns.

p16. Function boundaries
Write 5 tasks where a small problem must be split into 2 to 3 helper functions.
Focus on clean inputs, outputs, and return values.

p17. Default arguments and sentinels
Write 5 tasks that require default parameters, optional behavior, and a sentinel object.
Make sure the function keeps its syntax readable.

p18. Loops with state
Write 5 tasks that need a loop, an accumulator, and one or more break/continue decisions.
Each task should be solvable without external libraries.

p19. Comprehensions and filters
Write 5 tasks that can be solved with list, dict, or set comprehensions.
At least 2 tasks should require a nested condition.

p20. Generators
Write 5 tasks that use yield or yield from.
At least 1 task should turn a list-producing function into a lazy generator.

p21. Closures and nonlocal
Write 5 tasks where an inner function remembers or updates outer state.
Use closure syntax deliberately instead of globals.

p22. Decorators
Write 5 tasks that wrap a function for logging, validation, timing, or retry behavior.
At least 1 task should preserve metadata with functools.wraps.

p23. Exception flow
Write 5 tasks that raise, catch, re-raise, or transform exceptions.
Include one task with custom exception classes.

p24. String parsing
Write 5 tasks that parse text with split, partition, slicing, or formatting.
Keep the implementation pure Python.

p25. Recursion
Write 5 tasks that are naturally recursive, such as tree walks or nested counting.
At least 1 task should include a base case and a guard against bad input.

p26. Data modeling
Write 5 tasks that define a small class with __init__, __repr__, and one custom method.
Use properties or computed attributes where they make the code clearer.

p27. Operator methods
Write 5 tasks that implement dunder methods such as __len__, __iter__, __eq__, or __add__.
The goal is to make custom objects behave naturally in Python expressions.

p28. Context managers
Write 5 tasks that use with, __enter__, and __exit__.
At least 1 task should manage a resource-like object without touching external packages.

p29. Pattern matching
Write 5 tasks that use match/case to branch on shape, value, or type.
Include one task with a catch-all case and one with guard clauses.

p30. Sorting and ordering
Write 5 tasks that sort data using key functions, lambda expressions, or tuple ordering.
Make the ordering rules explicit in code.

p31. Iterables and iterators
Write 5 tasks that distinguish between an iterable, an iterator, and a consumed iterator.
One task should define a custom iterator class.

p32. Mini parsing engine
Write 3 tasks that interpret a small command language or token stream.
Use only built-in Python syntax and control flow.

p33. Refactor challenge
Write 3 messy Python snippets and refactor them into cleaner versions without changing behavior.
Focus on syntax, structure, and readability, not on libraries.

Rules for every exercise
Use Python only.
Do not include solutions in this file.
Keep the drills difficult enough that you have to reason before coding.
Write answers in a separate scratch .py file.